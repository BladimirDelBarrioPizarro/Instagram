import React from 'react';

const Title = (props:any) => {

    const styleTitle = {
       color:'#555',
       
    }
    return(
        <h2 style={styleTitle}>Login</h2>
    )
}

export default Title;